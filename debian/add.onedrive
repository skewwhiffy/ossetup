#!/usr/bin/env bash

# sudo apt install -y 

mkdir -p ~/code/third.party
cd ~/code/third.party
rm -rf onedrive
git clone https://github.com/abraunegg/onedrive
cd onedrive

# make clean
# make
# sudo make install
