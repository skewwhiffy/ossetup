#!/usr/bin/env bash

../debian/ensure.nala
sudo nala install wget

echo Downloading Zoom
rm -rf zoom_amd64.deb
wget https://zoom.us/client/latest/zoom_amd64.deb

echo Installing Zoom
sudo nala install --assume-yes ./zoom_amd64.deb
